class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        ans = []
        for i,n in enumerate(nums):
            ans.append(n)
        for i,n in enumerate(nums):
            ans.append(n)
        return ans